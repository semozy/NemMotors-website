export interface FAQ {
  question: string;
  answer: string;
}

export interface Review {
  name: string;
  text: string;
  date: string;
}

export interface Service {
  title: string;
  description: string;
}
