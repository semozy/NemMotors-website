import { createClient } from "@supabase/supabase-js"; import fs from "fs"; const env = fs.readFileSync(".env.local", "utf8"); const url = env.match(/NEXT_PUBLIC_SUPABASE_URL=(.*)/)[1]; const key = env.match(/SUPABASE_SECRET_KEY=(.*)/)[1] || env.match(/SUPABASE_SERVICE_ROLE_KEY=(.*)/)[1]; const supabase = createClient(url, key); async function run() { const emails = ["zeehondzeb@gmail.com", "semihisleuk25@gmail.com"]; for (const email of emails) { const { data: admin } = await supabase.from("admin_users").select("id, user_id").eq("email", email).maybeSingle(); if (admin) { if (admin.user_id) await supabase.auth.admin.deleteUser(admin.user_id); await supabase.from("admin_users").delete().eq("id", admin.id); console.log("Deleted", email); } } } run();
